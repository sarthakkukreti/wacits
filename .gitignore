node_modules/
.env
.env.local
*.log
dist/
build/
.next/
.turbo/
.DS_Store
*.tsbuildinfo
# This project standardizes on Bun (bun.lock); Next.js's dev server can
# auto-install a stray pnpm lockfile the first time it runs.
pnpm-lock.yaml
# Packaged Hostinger deployment artifact (see apps/web/scripts) — a build
# output, never committed.
wacits-web.zip
# Mirror of apps/web/.next/static, written by prepare-standalone.sh so
# Hostinger's web server can serve /_next/static/* directly (see that
# script for why). A build output, never committed.
apps/web/_next/
